import {describe,expect,it} from "vitest";import {normalizeQuery,rankMatch} from ".";describe("search",()=>{it("normalizes Chinese whitespace and punctuation",()=>expect(normalizeQuery("  怀才　不遇，TEST ")).toBe("怀才 不遇,test"));it("ranks exact title above scenario",()=>{expect(rankMatch("东山再起",{title:"东山再起"})).toBe(100);expect(rankMatch("重新开始",{title:"东山再起",scenarios:["重新开始"]})).toBe(80)})});
