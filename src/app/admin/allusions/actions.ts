"use server";import { redirect } from "next/navigation";import { revalidatePath } from "next/cache";import { requireAdmin } from "@/lib/auth";import { saveAllusion } from "@/services/allusion.service";
export async function saveAction(formData:FormData){const session=await requireAdmin();const id=String(formData.get("id")||"")||undefined;const value=Object.fromEntries(["title","slug","pinyin","summary","meaning","story","usageNotes","status","qualityLevel"].map(k=>[k,formData.get(k)??""]));const result=await saveAllusion(value,id,session.user.id);revalidatePath("/");revalidatePath("/admin/allusions");redirect(`/admin/allusions/${result}?saved=1`);}
