import { notFound } from "next/navigation";import { requireAdmin } from "@/lib/auth";import { AllusionForm } from "@/components/admin/allusion-form";import { getAdminAllusion } from "@/services/allusion.service";export const dynamic="force-dynamic";export default async function Edit({params}:{params:Promise<{id:string}>}){await requireAdmin();const item=await getAdminAllusion((await params).id);if(!item)notFound();return <section className="admin"><h1>编辑 · {item.title}</h1><AllusionForm item={item}/></section>}
